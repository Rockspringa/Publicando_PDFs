import { UserPhotoDirective } from './user-photo.directive';

describe('UserPhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new UserPhotoDirective();
    expect(directive).toBeTruthy();
  });
});
